# Step 3 — Sitemap — Full Billing

```
Settings
└── Billing (tab, alongside General / Departments & Teams / Plan)
    ├── Current plan + seats used/limit + next invoice date
    ├── Payment method (update dialog)
    ├── Request more seats (dialog)
    ├── Cancel subscription (confirm dialog)
    └── Invoice history (list)
```

No new top-level route or nav item — this lives inside the existing Settings surface, same as General and Departments & Teams.

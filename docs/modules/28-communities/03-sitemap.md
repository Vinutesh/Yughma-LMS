# Step 3 — Sitemap — Communities

```
Course detail (existing)
└── Discussion (new tab, alongside Content/Outline)
    └── Thread detail

Learning-mode nav
└── Community (new item) — org-wide feed
    └── Thread detail

Manage-mode nav
└── Moderation Queue (new item, Team section — courses:edit gated)
```

Two new nav items total: "Community" (Learning mode, everyone) and "Moderation Queue" (Manage mode, `courses:edit`). This is the first Learning-mode addition since the consolidated nav review in Phase 3 — worth another look at grouping once it lands, same discipline applied last time.

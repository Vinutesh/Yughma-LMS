# Step 3 — Sitemap — Calendar

```mermaid
flowchart TD
    Calendar[Calendar — Month/Week/Agenda views] --> DeadlineDetail[Event detail: deadline, read-only]
    Calendar --> ManualDetail[Event detail: manual event, editable]
    Calendar --> AddEvent[Add event form]
```

## Carried into Step 4 (Wireframes)

Calendar (month view — the other two view toggles are layout variants, not redrawn), event detail (both variants), add-event form.

# Open Questions — Content Library

No new questions — same narrowing pattern as Phase 0:

- **In scope:** Media library (grid, with folder + tag filtering), Upload, Content detail/preview, Delete confirm, Move to folder.
- **Deferred:** "Reusable content blocks" (a more advanced authoring concept — pre-built content snippets reusable across lessons) from [MODULES.md](../../MODULES.md) §9 — real value, but it presumes Lessons' authoring UI already exists, which it doesn't yet in this pass. Revisit once Lessons is built and real reuse patterns are visible.
- **Replace-file** isn't a separate modal — folded into the Content detail panel as an inline action, consistent with keeping states inline over separate screens.
- **Confirmed foundational role:** this module is built and designed *before* Courses/Lessons specifically so their upload flows can point at one shared library instead of each inventing their own (the DAM architect note from [MODULES.md](../../MODULES.md) §9).

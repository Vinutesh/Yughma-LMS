# Step 3 — Sitemap — Integrations

```
Manage
└── Integrations (new nav item, Organization section)
    ├── Directory (grid of integration cards)
    ├── Per-integration config panel (generic, reused)
    ├── Webhooks (tab)
    └── API Keys (tab)
```

One new Manage-mode nav item — "Integrations" — under Organization, alongside Users/Roles/Audit Log/Settings/Plan.
